(function (deps, factory) {
    if (typeof module === 'object' && typeof module.exports === 'object') {
        var v = factory(require, exports); if (v !== undefined) module.exports = v;
    }
    else if (typeof define === 'function' && define.amd) {
        define(deps, factory);
    }
})(["require", "exports", "./dynamicAnchor", "../../compile/util"], function (require, exports) {
    {
        CodeKeywordDefinition;
    }
    from;
    "../../types";
    var dynamicAnchor_1 = require("./dynamicAnchor");
    var util_1 = require("../../compile/util");
    var def = {
        keyword: "$recursiveAnchor",
        schemaType: "boolean",
        code: function (cxt) {
            if (cxt.schema)
                dynamicAnchor_1.dynamicAnchor(cxt, "");
            else
                util_1.checkStrictMode(cxt.it, "$recursiveAnchor: false is ignored");
        },
    };
    exports.default = def;
});
